# Archived Expo / React Native version

This folder contains the earlier Expo / React Native implementation of Open-Sesame.

It is kept only for historical reference. The active app is now the native Kotlin Android project in the repository root.

Use the root-level Gradle project for builds and releases.
